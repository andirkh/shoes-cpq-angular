import { JaCurrencyPipe } from './ja-currency-pipe';

describe('JaCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new JaCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
